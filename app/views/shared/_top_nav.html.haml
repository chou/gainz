%nav{:class => 'top-bar'}
  %ul{:class => 'title-area'}
    %li{:class => 'name'}
      %h1
        =link_to 'Gainz ', ''
    %li{:class => 'toggle-topbar menu-icon'}
      %a{:href => '#'}
        %span Menu
  %section{:class => 'top-bar-section'}
    <!-- Right Nav Section -->
    %ul{:class => 'right'}
      - if current_user
        %li
          = link_to "Hi, #{current_user.first_name}", '#'
        %li
          = link_to 'Eat', eat_index_path
        %li
          = link_to 'Sign Out', destroy_user_session_path, method: :delete
      - else
        %li
          = link_to 'Make Gainz', new_user_registration_path
    <!-- Left Nav Section -->
    %ul{:class => 'left'}
      %li
        %a{:href => '#'} Left Nav Button
